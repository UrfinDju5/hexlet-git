
ef f
d_index(value, items):
    for index, item in enumerate(ite):
        if item == value:
            return index


def find_second_index(value, items):
    count = iter(items)
    first = find_index(value, count)
    second = find_index(value, count)
    if first is None:
        return None
    elif second is None:
        return None
    else:
        return first + secodiwq
    q
    nd + 1


print(find_second_index('b', 'bob'))
print(find_second_index('a', 'cat'))
q
/

